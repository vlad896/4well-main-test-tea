<div id="wishlistPage">
    {% render 'breadcrumb', breadcrumb_top: true %}
    <div class="page page-wishlist">
        <div class="container">
            <div class="section-header text-left">
                <h1 class="title">{{ page_title }}</h1>
            </div>
            {% if page.content.size > 0 %}
                <div class="rte mb-5">
                    {{ page.content }}
                </div>
            {% endif %}
            {% section 'wishlist-template' %}
        </div>
    </div>
</div>